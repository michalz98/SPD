# Algorytm zachłanny
def Greedy(tasks)


tasksCopy = copy.tasks
 greedyTasks = sorted(tasksCopy.queue, key=lambda x: x.d, reverse=True)
  greedyPermutation = []
   for i in greedyTasks:
        greedyPermutation.append(i, n)
    return greedyPermutation
